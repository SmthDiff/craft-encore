{% import "_includes/forms" as forms %}

{{ forms.textField({
    label: 'Public Path',
    instructions: 'The path of the public directory containing the index.php.',
    id: 'publicPath',
    name: 'publicPath',
    value: settings.publicPath,
    required: true
}) }}

{{ forms.textField({
    label: 'Asset Path',
    instructions: 'The path of the asset directory where Webpack Encore stores the compiled files.',
    id: 'assetPath',
    name: 'assetPath',
    value: settings.assetPath,
    required: true
}) }}

<hr>

<h3>Plugin Usage</h3>

<p>Add versioned CSS files.</p>
<code>
    <pre>
{{ '{{ craft.encore.entryLinkTag(\'app\') | raw }}' | e }}
    </pre>
</code>

<p>Add versioned JS files.</p>
<code>
    <pre>
{{ '{{ craft.encore.entryScriptTag(\'app\', true) | raw }}' | e }}
    </pre>
</code>
